stages:
  - one
  - two

jobs:
  first:
    stage: one
    runner: RUNNER
    image: IMAGE
    only: branch == master
    env:
      a: a
      b: 1
    commands:
      - echo hej
      - echo hej
  second:
    stage: two
    runner: RUNNER
    image: IMAGE
    env:
      a: branch
      c: c
    only: branch == master
    commands:
      - hej
      - hej